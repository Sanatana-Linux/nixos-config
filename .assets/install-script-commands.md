From the Installation ISO run:

```bash
nix-env -iA nixos.wget
wget https://raw.githubusercontent.com/Sanatana-Linux/nixos-config/main/.assets/install.sh -O install.sh
chmod +x install.sh
bash install.sh
```
