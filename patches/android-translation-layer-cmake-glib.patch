--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -15,8 +15,8 @@ set(CMAKE_C_STANDARD 11)
 
 # Find required packages
 find_package(PkgConfig REQUIRED)
-pkg_check_modules(GLIB REQUIRED glib-2.0)
-pkg_check_modules(GTHREAD REQUIRED gthread-2.0)
+pkg_check_modules(GLIB REQUIRED glib-2.0>=2.0)
+pkg_check_modules(GTHREAD REQUIRED gthread-2.0>=2.0)
 
 # Add compiler flags
 set(CMAKE_C_FLAGS "${CMAKE_C_FLAGS} -Wall -Wextra")
@@ -28,6 +28,9 @@ if(CMAKE_BUILD_TYPE STREQUAL "Debug")
 endif()
 
 # Include directories
+include_directories(SYSTEM ${GLIB_INCLUDE_DIRS})
+include_directories(SYSTEM ${GTHREAD_INCLUDE_DIRS})
+
 include_directories(${CMAKE_SOURCE_DIR}/vendor/elfutils/libelf)
 include_directories(${CMAKE_SOURCE_DIR}/vendor/elfutils/libdw)
 include_directories(${CMAKE_SOURCE_DIR}/vendor/elfutils/lib)